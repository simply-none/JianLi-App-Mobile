# 模块：功能域清单与使用方式

> **2026-09-13 内容/工具 Tab「待办化」全量改造（本表各行细节以此为准补充）**：
> 内容（notes / conversation / ebook）与工具（twofactor / password_vault / file_vault / qr）九个功能页统一对齐待办列表页骨架——自定义头部（‹ 22 · 标题 18/Bold · 右上动作）+ 专属色 PageBanner（纹理 texture11 + ringDecor + AnimatedStat 统计，margin(16,0,16,0)）+ **吸顶搜索行 PinnedSearchRow/PinnedSearchHeader（shrinkOffset>0 铺 pinnedCover）** + SoftChip 条件行 + ScopeTabBar（conversation/ebook）+ StaggerList + 待办式空态。弹窗全量收敛三档制共享 SheetScaffold（lg 定高不扣键盘、禁 autofocus、底部按钮贴底、边距 16 一次），FTileGroup 操作菜单全换 showSheetActionMenu / showSheetConfirm。
> **功能对齐 PC**：notes 分页（10/页「加载更多」）+ 标签管理抽屉（重命名/改色/软删，`renameTagDef`/`updateTagColor`）+ 编辑输入抽屉规范化；conversation 跨主题搜索（标题+内容+标签）+ 主题标签筛选/管理（`updateTag`/`deleteTag`，删除自动从主题/对话 tags 移除）+ 主题编辑 lg 化 + 长按菜单化；ebook 排序（最近阅读/添加时间/书名，SharedPreferences 持久化）+ 网格/列表双视图（`_BookRow`）+ 搜索书名作者；twofactor 添加三方式（手动/相机扫码 `_OtpScanPage`/粘贴 URI）+ 编辑账户（算法/位数/6·8 位/30·60s 周期）+ 导出 otpauth 二维码（QrImageView + 复制链接）+ 删除（`updateAccount`/`removeAccount` 重加密回写）；password_vault 条目字段补齐（category/otpSecret/createdAt，向后兼容解码）+ 密码生成器（随机：长度 8-64+字符集+排易混淆；口令短语：词数 3-8+分隔符，Random.secure）+ 条目详情 lg（脱敏+显示明文+TOTP 实时码）+ 复制 30 秒自动清空（`copyWithAutoClear`）+ 分享 vault 文件（share_plus）；file_vault 导出/分享（解密到临时文件经分享面板）+ 立即锁定 + 预览 lg 化 + 大小人性化显示（size 为 TEXT 列需 int.tryParse）；qr 内容类型补齐 9 种（+短信/电话/位置/日历事件，`buildQrPayload` 加 extra 字段表）+ 前景/背景色板+容错率 L/M/Q/H + 分享 PNG（QrPainter.toImageData）+ 历史来源标识（本机/桌面端）+ 清空本机历史；新增 `/about` 关于页（品牌 hero + 功能总览三域 + 双端协同 + 安全与隐私 + 依赖鸣谢分组 + 版本号 `kAppVersion`）；设置面板卡片化（AppCard 包 FTileGroup + 分区图标 + 版本脚注）且「关于」可点击跳转。
> **共享原子升级**：`SoftChip` 增加选择形态 `onTap`（无 ×，TapScale 包装）；`SheetInputBox`/`SheetMultilineBox` 增加 `onChanged`（页内实时预览联动）。AppCard 增加 `onLongPress`。**零表结构变更**（PC 主题对话「置顶」为消息级 pinned，移动端早已实现；主题表无 pinned 列，无需迁移）。


## 功能域清单与状态
| 功能域 | 路由 | 状态与要点 |
|---|---|---|
| home | `/` | Dashboard 聚合：习惯/待办/专注/提醒统计 + 最近倒计时 + 快捷入口；**Phase 3 视觉重设计**（渐变英雄卡 + 专属色磁贴 + StaggerList）；右上角**设置按钮**；**2026-09-16 新增【提醒守护】栏**（效率概览与快捷入口之间，共享 `ReminderGuardCard`，与提醒列表页共用单一真源）；**2026-09-19 数据自动刷新**（切 tab 返回 / 功能页 pop 返回 / App 回前台时 invalidate `dashboardStatsProvider`，见 changelog 当日条目）** |
| habit | `/habit` | 今日打卡 + 幂等切换（key=`habitKey#date`）+ 近 7 天；**视觉焕新**：**PageBanner 绿(2)**（启用/今日完成统计）+ 条目专属色 `SquircleBox` 图标盘 + `AnimatedCheck` + StaggerList；**交互**：长按列表项 → 50vh 操作菜单（编辑/删除，删除二次确认 `showSheetConfirm`），单击 → 80vh 详情（底部「编辑习惯」+「删除习惯」）→ 编辑 80vh 弹窗（`updateHabit` 联动提醒重建）；**提醒可靠性（2026-09-16）**：提醒排/取消统一改 `NotificationService.stableId(id)`（原 `id.hashCode` 重启后可能变 → 旧通知取消不到），并新增 `HabitRepository.rescheduleAll()` 纳入 `healAlarmSchedules` 自愈（被系统清理后随手开 App 即重建），rescheduleAll 先取消旧版 hashCode 残留（含每周变体）再重建，逐条 try/catch |
| todo | `/todo` | **已对齐 PC（2026-09-07，全量重写，详见「待办」专节）**：三视图 list/card/calendar（`JianliSegmented`）；搜索 + 筛选抽屉（优先级/状态/标签/显示已完成·模板/分组 none·status·due·parent）；全量统计 PageBanner 蓝(1)；分组（状态/到期 overdue·today·tomorrow·thisweek·later·nodate/父任务带子任务进度）；全字段（优先级/到期/状态 6 态/截止提醒→本地通知（2026-09-16 加固：cancel/schedule 改 `stableId(key)`、新增 `TodoRepository.rescheduleAll()` 纳入 `healAlarmSchedules` 自愈，被系统清理即重建，rescheduleAll 先取消旧版 hashCode 残留）/重复 daily·weekly 自动生成实例/父任务多选/标签多选/子任务级联）；记录进展→主题对话；动作/确认/编辑/筛选/日期/标签/父任务/**详情**全部底部抽屉（**详情 + 编辑同挂 lg 档 → 高度必然一致**，2026-09-12）；**单击条目 = 只读详情**（要改再点详情「编辑」，三入口共用 `openTodoDetail`）；弹窗高度一律走**三档制 30/50/80%**（`SheetSize`，见 `interaction-patterns.md`）；**列表滚动吸顶以搜索行为锚点**（搜索框常驻顶部，横幅/条件 chip/Tab 栏随滚动移出；`_PinnedHeader`，2026-09-12；卡片/日历视图仍是 Column 固定头，待统一）。表定义零改动、无需迁移；**视觉/交互修正（2026-09-12）**：列表页背景与内容区分离（`_PinnedHeader` 用 `overlapsContent` + `AppTokens.pinnedCover`，背景不再被下方内容盖住，整屏统一为设计图 02 背景）、新增/编辑输入框常态可见描边且聚焦高亮主题色、描述框随输入增长、弹窗与页面左右 padding 统一收口 16（FHeader 标题同步由 12→16） |
| pomodoro | `/pomodoro` | **2026-09-13 重构：计时改为「页面内本地计时」（用户拍板：移动端逻辑独立于 PC，用主流番茄钟应用方案；原持久状态机 / `startTime` / 原生阶段通知全部删除）**。① **计时**：只有点「开始专注」才起表（`_PomodoroPageState` 内存态 `_started/_running/_phaseIndex/_remainingSeconds` + `Timer.periodic(1s)`）；**离开页面（`dispose`）或 App 切后台（`WidgetsBindingObserver` 的 `paused`/`hidden`）立即停表并复位到空闲**；每次「重新开始」= 全新一轮（专注满时长起）；阶段完成自动进入下一阶段（专注↔休息循环）并写一条 `pomodoro_status` 流水（value=work/rest，供首页统计与记录弹窗）。⚠️ `inactive`（下拉通知栏/权限框）不触发停止。② **头部统一** `‹ 番茄钟 [记录][设置]`（三种展示效果一致）：**删 ⟳ 横竖屏循环与 `pomodoro.orientation` 偏好**；记录=`history` 图标 → `showPomodoroRecordsSheet`；设置=原页底 ⚙ 移到头部（长按整页仍同入口）。③ **底部统一** `[开始专注/暂停/继续] [重新开始]`（空闲=开始 / 运行中=暂停 / 已暂停=继续）。④ **记录弹窗（新）** `components/pomodoro_records_sheet.dart`：`showPomodoroRecordsSheet` 走 `showFSheet`（lg 80vh 定高）——把手 + 「番茄钟记录」+ 统计行 + 可滚周期列表（每条=完成的阶段，类型 SoftChip 专注红(6)/休息绿(2)）+ **底部固定【导出】**（`exportTextToDownloadDir` → `Download/渐离App导出/番茄钟记录_*.md`，正文见 `utils/export_pomodoro_markdown.dart`）；**旧独立记录页 `/pomodoro/records` + `pomodoro_records_page.dart` 已删除**。⑤ **提示音**：阶段完成时 `NotificationService.showNow`（番茄钟渠道 playSound + 横幅，**比 `SystemSound.play` 可靠**——后者依赖系统「触摸音效」开关）+ `haptic(success)` + toast。**双保险（2026-09-16）**：页面内计时被进程杀掉即失效，故开始专注/继续/重新开始/进入下一专注阶段额外排 `NotificationService.scheduleOnce(id: stableId('pomodoro:end'), channelKey: pomodoro, precise:false)` 原生一次性系统通知（非精确、allowWhileIdle 息屏可响）；专注自然结束（`_completePhase` 且 finished=work）取消它（前台已用 `showNow` 提示），避免重复；显式暂停/离开页面（`_stopAndReset`）也取消。**进程被杀不触发生命周期回调 → 原生通知照常到点弹出，覆盖「被杀不提醒」主因**。⑥ **数据/仓库**：`PomodoroRepository` 只剩 `loadDisplay/saveDisplay/loadStates/updateDurations/recordStatus`（删 `startRound`/`loadSnapshot`/`reschedulePhaseNotifications`/`_phaseIds`）；`pomodoro_state_machine.dart` 只剩 `PomodoroStateDef` + `parsePomodoroStates`（删 `PomodoroSnapshot`/`computePomodoroSnapshot`）；`alarm_bootstrap.dart` 删番茄钟那一步。⑦ **三种展示效果**（basic_info 键 `pomodoro_display`，设置弹层 choiceChip）：`normal` 普通=横幅 + 白卡进度环（**竖屏：计时卡 `Expanded` 撑满剩余高度、内容垂直居中**，2026-09-13 用户实指；横屏=左横幅 + 右大环）／`clean` 清爽=仅进度环内容卡（无说明文字，锁竖屏）／`landscape` 横屏=**大字倒计时 HH:mm:ss**（`FittedBox(scaleDown)` 兜底，锁横屏）；**展示效果即方向**，离开页面恢复跟随系统。⑧ **首页联动**：`dashboard_providers.dart` 新增 `pomodoroWorkMinutes`（读 states 的 work 段，缺省 35），「今日 X 轮 · Y m」不再硬编码 ×25。⑨ **周期规则（2026-09-13 二轮，用户拍板，写 `basic_info` 键 `pomodoro_cycle_rule`）**：设置弹层「周期规则」二选一（`restart` 未完成重新开始＝默认／`resume` 未完成继续上一轮）。「未完成」＝一轮结束前离开页面或切到其他应用；**仅作用于专注阶段**（休息阶段未完成一律丢弃）。`restart` → 丢弃进度、下次从专注满时长起；`resume` → 把专注剩余秒数写 `basic_info` 键 `pomodoro_progress`（`{phase:0,remaining:N}`，**不限有效期**），重进页面/切后台回前台时**还原为「已暂停」**（底部按钮显示「继续」，点它才接着走 —— 仍守「手动开始才计时」）。进度清除时机：走完一轮 / 点「重新开始」/ 规则切回 `restart`。⚠️ `dispose()` 里 `ref` 不可再安全访问 → 页面缓存 `_repoCache`；Android 切后台连发 `hidden`+`paused` → `_roundActive` 开关保证「离开」只处置一次；「切后台→立刻返回」有内存暂存 `_pendingResumeSeconds` 兜底，不依赖异步落盘时序。⚠️ reminders 表**不在同步白名单**，时长配置只在本机生效 |
| reminder | `/reminders` | **通知渠道全量悬浮横幅（2026-09-18，用户要求「所有提醒像倒计时一样在屏幕上直接看到」）**：五个渠道统一 `importance: NotificationImportance.High`（= 悬浮横幅 / 前置屏幕可见，不必下拉通知栏）+ `enableVibration` + `defaultPrivacy: NotificationPrivacy.Public`（锁屏全文）；`countdown`/`alarm` 本来就是 High（倒计时即基准）。⚠️ **渠道重要性创建后永久锁定**（同名重建被软删除恢复旧设置）⇒ 只能换键：`habit`→`habit_v2`、`todo`→`todo_v2`、`pomodoro`→`pomodoro_v2`，旧键 `init()` 里 `removeChannel` 删除，历史 payload 旧键走 `migrateChannelKey()`；原生周期「通知」渠道 `reminder_notify` 本就 HIGH。详见 SKILL 红线 #22。**Android 15 真机三条断链修复（2026-09-18，红线 #26）**：① 闹钟不再 `getActivity` 直拉 Activity（`targetSdk=36` ≥ 35 ⇒ Android 15 起 PendingIntent **创建者**默认不委托 BAL，被**静默拦截**、只有 `logcat` 有 `Background activity launch blocked!`；Android 14 创建者隐式委托，故模拟器全正常）→ 改**广播 + 全屏意图通知**：`AlarmScheduler` 用 `getBroadcast` 挂 `AlarmRingReceiver`（alarm）/ `ReminderAlarmReceiver`（notify），接收器发带 `setFullScreenIntent` 的高优通知，**由系统拉起 `AlarmRingActivity`**（系统自持 BAL 特权），并**自排下一次、不再依赖用户点「停止」**；② 清单加声明 **`USE_EXACT_ALARM`**（安装即授予）解决「未授权 → 回退 awesome 不精确 → 周期飘 / 定点迟」；③ 守护抽屉新增「前置显示」引导块 + **从系统设置页返回即强制重排一次**（绕开 `app.dart` 的 5 分钟节流，让刚授予的权限立刻生效）。**已对齐 PC newTips（2026-09-09 全量重写）**：三模式 time/interval/stateful；**送达双模式** `delivery`（'notification' 系统通知（awesome，精确权限缺失自动降级为不精确）/ 'alarm' 闹钟：**2026-09-16 起走原生 AlarmManager.setAlarmClock**（见 SKILL 红线 #20），不再用 awesome preciseAlarm（Android 12+ 未授权 SCHEDULE_EXACT_ALARM 会抛 SecurityException → 闹钟从不响）；setAlarmClock 息屏/Doze 必响、锁屏全屏，重复类由**原生广播接收器**（`AlarmRingReceiver`/`ReminderAlarmReceiver`）收到后**立即自排下次**（2026-09-18 起不再依赖用户点「停止」），连杀进程也持续；⚠️ **2026-09-18 纠错：`setAlarmClock` 同样需要 `SCHEDULE_EXACT_ALARM`**（官方文档把 `setAlarmClock()` 与 `setExact()`/`setExactAndAllowWhileIdle()` 并列；Android 12+ 未授予抛 `SecurityException`，13+ 新装默认不预授予），故**原生桥失败必须回退 awesome**（见 SKILL 红线 #21），此前「无需精确闹钟权限」的记法是错的；**2026-09-18 已声明 `USE_EXACT_ALARM`**（Android 13+ 安装即授予、用户不可撤销）拿到精确能力，兜底仍保留）；time 模式支持 每天/每周/一次性(date)/每小时/每月/每年，weekDays 用 PC 约定 0=周日…6=周六（awesome 1=周日…7=周六→+1）；interval 模式每 N 分/时/天（**2026-09-17 起无论「通知」还是「闹钟」送达都走原生 `AlarmScheduler`**——awesome 的 `NotificationInterval` 在 Android 12+ 被 Doze 节流并自我停摆，表现「只响 2 次就停 + 间隔不准」：`mode='alarm'` 指向 `AlarmRingActivity`、`mode='notify'` 指向 `ReminderAlarmReceiver`，收到即发普通通知并按 `repeatSpec.interval` 自排下次；**2026-09-18 起原生返回 false 时回退 awesome `scheduleInterval`**，见 SKILL 红线 #21）；免打扰 idleTime 段内顺延（一次性最佳，周期逐次精确跳过需后台 worker，标记近似）；过滤 `source==='todo'`；stateful 只读展示（番茄钟拥有）；进入页面 `rescheduleAll()` 重排程防 awesome 原生计划被清（另有冷启动 + **回前台 5 分钟节流自愈**，见本文件第 4 条）；**视觉焕新**：**PageBanner 琥珀(3)** + 专属色图标盘（闹钟红(6)/通知琥珀(3)）+ 新建/编辑底部抽屉 `SheetSurface`（JianliSegmented 选模式·送达·重复 + 星期 FButton + 免打扰开关）。**创建 UI 已开放全 6 种重复**（每天/每周/一次性/每月/每小时/每年，`_repeatIndex` 0-5 → `'daily'/'weekly'/'once'/'monthly'/'hourly'/'yearly'`；每月写 `dayOfMonth`、每年写 `month`+`dayOfMonth`，`date` 三者都留以便回显编辑）；列表副标题用 `ReminderItem.repeatLabel`。**闹钟增强（2026-09-09）**：① 重复响铃 —— `scheduleCalendar(extraRings: alarm?2:0)` 额外排 2 次顺延 1 分钟的响铃（id `+1000*k`，可单独取消；hourly 场景 hour 为 null 只回绕分钟）；② 稍后提醒 —— 闹钟（fullScreen=true）自动带 `actionSnooze`「稍后提醒」按钮 + payload，点击后 5 分钟再响且可连续贪睡（`init()` 注册 `setListeners(onActionReceived:)` → `_onActionReceived`，重排 id 用 `base+50000` 段避开主响铃与 ring 段）。**待办化改造（2026-09-12）**：页面 = 待办骨架（主色横幅四统计 全部/定点/周期/启用中 + 权限提示条 + 吸顶搜索行按标题/内容过滤 + Tab 全部/定点/周期/多状态 + 卡片 r16 带 SoftChip）；**提醒守护（2026-09-13 重做，取代原「权限提示条」）**：横幅下方**常驻**「提醒守护 N/4」卡（四项全就绪 = 绿 `circleCheck`「已就绪，到点会准时响起」/ 否则琥珀 `shieldAlert`「N/4 项已就绪，到点可能不响」+「去开启」）→ 点开 `ReminderGuardSheet`（公开共享组件 `reminder_guard_sheet.dart`，首页【提醒守护】栏同用此卡与弹窗；lg 80vh）：① 逐项开关行（图标 + 标题 + 后果说明 + 右侧「已开启」/「去开启」）= 通知权限 / 闹钟和提醒 / 忽略电池优化（`Permission.ignoreBatteryOptimizations`）/ 全屏通知（走原生 `jianli/system_actions`）；② **后台保活开关**（`FSwitch` + 运行状态行；说明常驻通知可在系统设置静音）；③ 各厂商保活路径清单（小米/华为/OPPO/vivo/三星）+ 「打开应用设置」`openAppSettings()`；④ 底部固定「完成」`GradientButton`。抽屉用 `WidgetsBindingObserver` 在 `resumed` 重查（跳系统设置页回来后不残留旧状态）。编辑弹层选「闹钟」时惰性 `requestExactAlarmPermission()`，未授权时在送达方式下方显示降级提示行「「闹钟和提醒」未开启，响铃可能延迟几分钟 · 去开启」；编辑弹层对齐图2（lg 定高 `SheetScaffold` + 字段组 label 14 + choiceChip 送达/模式/重复/周几 + 免打扰 switch-row + 底部固定 取消/创建 GradientButton，**无 autofocus**）；`AlarmBootstrap`（`lib/app/alarm_bootstrap.dart`）在 App 启动首帧后统一跑 `rescheduleAll`（原来只在进提醒页）；manifest 补 `POST_NOTIFICATIONS`/`RECEIVE_BOOT_COMPLETED`/`VIBRATE`；**长按卡片 → 操作菜单【编辑/停用·启用/删除】**（`showSheetActionMenu` sm 档；删除走 `showSheetConfirm` 危险确认；stateful=番茄钟托管行只读、不响应长按；停用/启用即 `toggleEnabled` 同步增删通知计划） |
| countdown | `/countdown` | 独立表（end_time 毫秒基准 + paused_remaining，与桌面同构抗休眠）+ 暂停/恢复/重置；**到点通知链已补齐（2026-09-12，对齐 PC「主进程到点→系统通知」）**：create/resume/reset 即排一次性系统通知（`countdown` 高重要渠道，`NotificationService.stableId('countdown:<key>')`，精确闹钟可用才 precise），pause/delete/finish 即取消；`sweepExpired()` 在 App 启动（AlarmBootstrap）与进页时把「running 且 end_time≤now」补写 finished；页面 tick 跨零检测 → 补写 + toast；**待办化改造**：主色 PageBanner（全部/进行中/已暂停/已结束）+ 大计时器改**白卡主色环**（原紫渐变英雄卡弃用，避免与横幅叠渐变）+ 吸顶搜索行（按名称）+ Tab 全部/进行中/已暂停/已结束 + 卡片 r16（状态 SoftChip：进行中主色/已暂停琥珀/已结束灰 + 44 进度环 + 暂停/重置/删除行内 ghost 钮）；新建/编辑共用 **`_CountdownFormSheet`**（lg 定高；**设定方式对齐 PC（2026-09-12）**：指定时刻=复用待办 `showTodoDateTimeSheet` 月历选择器；指定时长=年/月/日/时/分/秒六小输入框（年 365 天/月 30 天折算），默认 1 小时；编辑经 `updateTiming`——未改时间设定保留原 timing，改了回 running + finishedAt 清空 + 重排通知）+ **长按卡片 → 操作菜单【编辑/删除】**（删除走 `showSheetConfirm`；编辑与新建共用同一入口 `_openForm`） |
| notes | `/notes` | 列表（**关键词页内实时搜索 + 「查询抽屉」筛选**：分类单选/标签多选，已生效条件可点掉）/ 详情（flutter_widget_from_html 渲染 + 彩色标签徽标）/ 编辑（**方案 B 沉浸式编辑器（2026-09-18 落地，画布 `file/727189768661061` 选定 B）**：白底无卡 —— 26/w700 无边框大标题直排 → 元信息 chips 行（**纯展示**：已选分类 = 主色 12% 软底 `_CategoryMetaChip`、已选标签 = `NoteTagChip(selected:true)`；**不放「＋分类/＋标签」入口**，添加入口统一在底部条；**一个都没选中时整行 + 上下间距一起不渲染**）→ hairline 分隔 → 正文区 `Expanded`（默认**纯文本**：透明 `Material` + `TextField(expands:true)` 内部滚动；头部右侧 **Aa** 切 **flutter_quill** 富文本 → `QuillEditor.basic` + 正文下方单行横滑 `QuillSimpleToolbar`）→ 底部固定条「分类 n」「标签 n」入口 + 保存胶囊（`GradientButton(height:44, fullWidth:false)`）；分类/标签的多选与新建统一走 `showFilterSheet`（md 草稿闭包 + refresh）+ lg 输入抽屉）；**富文本可选、默认纯文本**：保存时富文本走 `rawHtml`（Delta→HTML by vsc_quill_delta_to_html）直落 `note_book.html`、桌面 vue-quill 直接可渲染，纯文本路径 = 旧段落化、与历史数据完全兼容；切回纯文本 toast 明示「样式不保存」。⚠️ 本页 **3 处必须自补透明 `Material`**（标题/正文 `TextField`、`QuillEditor` 选区菜单、`QuillSimpleToolbar` 的 `IconButton`）——`FScaffold` 不提供，漏一处即运行期崩，见 SKILL 红线 #23 ⑦）；**标签能力已全量对齐 PC**（内容+标签双搜索、多选筛选、新建/软删，见「笔记标签双端契约」小节）；**视觉焕新**：PageBanner 琥珀(3)（结果数/标签数统计）+ 卡片彩色标签徽标 + StaggerList |
| conversation | `/conversation` | **已对齐 PC（2026-09-05，同步已打通——见「主题对话」专节）**：主题列表（update_time 倒序 + 消息数角标 + 主题标签彩色徽标）/ 新建·编辑主题（底部抽屉+固定保存条）/ 删除主题（子主题禁止+级联删消息）/ 消息流（置顶排前 + is_rich 走 HtmlWidget 渲染 PC 富文本 + 长按软删）/ 发送追加记录；LLM 后端未定；**视觉焕新**：PageBanner 粉(4) + StaggerList + 粉软底气泡。**「对话记录」页（`ConversationMessagesPage`，2026-09-13）**：右上角 `suffixes` 导出按钮 → 导出**本主题全部对话记录**为 Markdown（`buildThemeMarkdown` → `Download/渐离App导出/主题-<标题>_<stamp>.md`；主题对象用 `ref.read(conversationThemesProvider.future)` 等首帧取，**不能用 `.value`**）；底部输入条 = `SheetInputBox`(h40) + 自绘 40×40 主色渐变方形发送钮（等高，见 interaction-patterns §4.6），回车走 `SheetInputBox.onSubmitted`。**富文本已对齐 PC（2026-09-18，红线 #25）**：快速输入栏 `SheetInputBox` 前新增 **Aa 按钮**（`TapScale` 40×40 muted + `FLucideIcons.type`）→ 独立**沉浸式富文本编辑页** `ConversationComposePage`（路由 `/conversation/:id/compose`，查询参数 `messageId`/`tags`/`refs`；**不用底部抽屉**——lg 档按红线 #9 不扣键盘，键盘会盖住编辑区、工具条无处安放；独立页随键盘收缩、工具条常驻键盘上方，与笔记编辑页方案 B 同构**含红线 #24 三条 quill 键盘铁律**）——只负责内容：标签/引用草稿由快速输入栏带入（`ConvTagBadge`/`ConvRefDraftChip`，可点掉），编辑已有对话时只改内容（对齐 PC `ConversationEditDialog`）。**工具条 1:1 对齐 PC 十项**（加粗/斜体/下划线/删除线/引用/代码块/有序·无序列表/链接/标题1-3 + 移动端补撤销重做），其余按钮全关（PC 气泡 CSS 只覆盖 `p/strong/em/u/s/a/h1-3/ul/ol/li/blockquote/pre`）。**写入走 `core/text/rich_text.dart::normalizeContent` 归一化**（与 PC `richText.ts` 逐字对齐）→ 只打纯文字会自动降级 `is_rich='0'`，不留半脏行；长按消息菜单新增 **【编辑内容】**（`squarePen`）→ `updateMessageContent`；三处内联摘要收口到 `snippetOf`/`plainLineOf`。**零表变更**（`conversation.is_rich` 两端早已有且在同步白名单） |
| ebook | `/ebook` | **打开空白/一直转圈已修（2026-09-05）**——根因：书架页 build 内联 StreamProvider（雷区 #9），改顶层 `bookshelfStreamProvider`；TXT 编码对齐 PC（BOM/UTF-8 严格/GBK 回退，`fast_gbk` 纯 Dart 包）；移出书架走 showFDialog 确认；组件化拆分 `book_cell.dart`。file_picker 导入 → sha256 content_hash 身份键 → epubx / TXT 正则分章 → 章节渲染 + 按章进度；**裁剪未做（PC 有，列 P2）**：标注划线 / 书签 / 分类 / PDF 渲染 / 章节搜索 / 扫描文件夹 / EPUB 内联 color·background 强制（夜间·护眼主题下 epub 内联白底会盖掉主题背景，需 force-styles）；**视觉焕新**：PageBanner 青(5) + CustomScrollView+SliverGrid 封面网格；阅读器正文 pageTint 护眼底；**导入容错加固（2026-09-09）**：file_picker 取消选择返回 `null`（非「空列表」），`_import` 先判空再取 path，整段 try/catch 把导入异常以「导入失败：$e」toast 显式暴露（此前无 try/catch → 异常被 Future 吞/崩溃，表现为「导入都报错」）；`importBook` 对不支持/缺失返回 null、I/O·DB 错误向上抛交给 `_import` 暴露。**阅读设置对齐 PC（2026-09-09）**：新增 `providers/reader_settings.dart`（`ReaderSettings` 字号/行距/主题 `day·night·eye` + `StateNotifierProvider`，`shared_preferences` 持久化，主题配色严格对齐 PC `themePresets.ts`：day `#ffffff/#333333`、night `#1a1a1a/#cccccc`、eye `#c7edcc/#2c3e50`）；阅读器监听该 provider，正文 `ColoredBox` 套主题背景 + `HtmlWidget` textStyle 应用文本色/字号/行距，顶栏设置按钮 → 底部抽屉 `JianliSegmented` 切主题 + 加减按钮调字号(12~36)/行距(1.2~3.0)；**TXT 复用此阅读器**（无独立 TxtReader，`parseBook` 已按扩展名分派），补齐主题后与 PC 核心体验对齐。**电子书全表同步 + 跨端传书（2026-09-09）**：① 七张 ebook 表全量进同步白名单——移动端 `kSyncableTables`（`core/sync/sync_service.dart`）、PC 端 `SYNCABLE_TABLES`+`tablePk`（`electron/main/module/sync/syncModule.ts`）、PC 端 `SYNC_TABLES`（`src/store/useSync.ts`），**三处必须同步加**，漏一处就「手机上表能推、PC 拒收/反之」；主键按表适配：`ebook_bookshelf`/`ebook_progress`=`file_path`、其余 `ebook*`=`id`、**`ebook_book_category`=`book_path, category_id`（`ON CONFLICT` 支持多列，复合主键同样幂等）**。② **跨端传书**：复用 47124 数据面新增对称三端点 `GET /ebook/list`、`GET /ebook/download?path=`、`POST /ebook/upload?name=&format=`（手机端 `features/ebook/services/ebook_transfer.dart`，PC 端 `electron/main/module/ebookTransfer.ts`，两端都用各自的可插拔路由钩子：`registerRouteHandler` / `registerDataRoute`）；身份键是内容 sha256（`importBookBytes` 与 PC 侧同名去重逻辑），PC 接收落盘到 `userData/jianli-books`。⚠️ **同步的固有坑**：`ebook_bookshelf` 主键是 file_path，而 PC 绝对路径 ≠ 手机沙盒路径 → 同步后同一本书会出现两行，故 `watchBookshelf` 按 `content_hash` 去重（**优先保留本机文件存在的那条**，`_dedupeByHash`）；打开文件前 `File.existsSync` 校验，缺失时提示「用传书从电脑导入」。⚠️ 列类型不一致：`ebook_bookmark.percent` 桌面端 REAL、移动端 TEXT（同步走 `toString()` 写入，SQLite 亲和性可容忍）。**P2 补齐（2026-09-09，直接按清单执行）**：① **书签**——`ebook_repository` 加 `watchBookmarks/findBookmark/addBookmark/removeBookmark`（按 `chapter:<i>` 锚定），阅读器顶栏书签按钮 → 底部抽屉 `SheetSurface` 列全部书签（跳转/删除）+「收藏当前章/取消收藏」；② **批注/划线**——加 `watchAnnotations/addAnnotation/removeAnnotation`，阅读器「批注」抽屉按当前章节锚点存 `type=markStrong`(黄) 或 `note`（可选笔记），列表可删；③ **分类**——加 `watchCategories/addCategory/assignCategory/unassignCategory/watchAllBookCategories/removeCategory`，书架顶栏「标签」→ 分类管理抽屉（新建/删除分类 + 逐书本勾选分类），顶部 chips 按分类筛选，书格显示分类标签（`book_cell.dart`）；④ **全文搜索**——阅读器「搜索」抽屉跨章节匹配并定位跳转（去标签取片段）；⑤ **EPUB 强制样式**——夜间/护眼主题下 `_renderHtml` 用正则剥离章节 HTML 内联 `color/background` 声明，避免内联白底盖掉主题背景（day 主题原样）。⚠️ **PDF / 扫描文件夹按用户要求暂不实现**。**笔记标注页（2026-09-10）**：长按书格 → 动作菜单「笔记标注」→ push `/ebook/notes`（新 page `components/book_notes_page.dart`），按 `content_hash` 汇总全书 `ebook_annotation`，三档（全部/划线/笔记）+ 统计行，**原文与笔记正文一律不截断**（无 maxLines/ellipsis），支持删除（底部抽屉确认）与「跳到该章」（路由 `chapter` 参数由 `EpubReaderPage.initialChapter` 消费，优先于已保存进度）；`book_cell` 长按回调改名为 `onMenu`，零改表。**forui API 雷区（本轮实踩）**：`FTile` 尾部是 `suffix`（**单数、单 Widget**，不是 `suffixes` 列表）；`FTextField` 无 `controller`/`onChange` 参数，文本用 `control: FTextFieldControl.managed(onChange: (v) => ...)` 且 `label` 取 `Widget?`（须 `Text(...)` 包裹）；`StatefulBuilder` 内的局部 `var` 会在每次 `setSt` 重建时被重置，**表单状态（adding/type/note/query）必须提到 `showFSheet` 方法作用域**而非 builder 闭包内。**书架卡片划线/笔记计数（2026-09-13）**：网格 `BookCell` 与列表 `_BookRow` 双视图均加 `markCount`/`noteCount`；数据源为新增的跨书全量流 `watchAllAnnotations()` + `allAnnotationsStreamProvider`（只开一条流，页面按 `content_hash` 分桶），口径走仓库顶层 `isNoteAnnotation()`（与笔记页「划线 N / 笔记 N」同源）；**排布（2026-09-13 二次修订）**：网格 `BookCell` 里「划线 / 笔记」**单独一行**居中（`已读 X%` 上一行，标题降到 `maxLines: 2`、网格 `childAspectRatio` 0.62→0.58 腾高度防溢出），列表 `_BookRow` 元信息行改 **两端对齐**（`Expanded(作者)` 靠左 + 已读/划线/笔记靠右，无作者时统计列也不左右横跳）。**导出（2026-09-13）**：右上角 ⬇ → `showBookExportSheet`（lg 80vh 抽屉，与「导出笔记」同构：搜索书名/作者 + 高级搜索（**分类 + 格式**两组多选，独立 80vh 抽屉）+ 勾选列表 + 底部三键【导出选中/导出筛选/导出所有】），选中色 `accent(5)` 青；导出 `.md`（书目 + 有批注的书附「标注与笔记」正文）落盘 `Download/渐离App导出/`，生成器 = `utils/export_books_markdown.dart`（纯函数），计数口径共用仓库 `annotationCountsByHash()`。**笔记标注页导出（2026-09-13）**：`book_notes_page.dart` 底部固定 `GradientButton('导出全部（N 条）')`（走 `FScaffold.footer` + `SafeArea(top:false)`，无内容时不渲染），导出**本书全部**标注与笔记（不受当前档位影响）→ `Download/渐离App导出/笔记标注_<书名>_<stamp>.md`，生成器 `buildBookAnnotationsMarkdown()`。 |
| twofactor | `/twofactor` | TOTP 全算法 + 门禁双模式（新建本机 vault / 导入桌面端 vault）+ 动态码卡片（复制/倒计时/锁定清内存）+ 添加三方式（手动/扫码/粘贴）；**导出**：解锁态头部 `⋯` 菜单导出 vault 到 `Download/渐离App 备份`（未授权回退沙盒）；**视觉焕新**：**PageBanner 紫(0)**（账户数）+ 门禁表单 pageTint+紫渐变图标盘 + `AccountCodeTile` 紫图标盘 |
| password_vault | `/password-vault` | 移动端口令库（同信封格式 .jlv）；**视觉焕新**：**PageBanner 蓝(1)**（条目数）+ 门禁表单 pageTint+蓝渐变图标盘 + 条目首字母蓝 `accent(1)` 渐变 SquircleBox + StaggerList |
| file_vault | `/file-vault` | 与 PC 完全兼容：wrappedKey 解包 + JLV1 parse + 导入/预览/删除；**视觉焕新**：**PageBanner 绿(2)**（文件数）+ 门禁表单 pageTint+绿渐变图标盘 + 文件行绿 `accent(2)` 渐变 SquircleBox |
| qr | `/qr` | 生成（9 种类型 text/url/wifi/vCard/email/sms/tel/geo/calendar）+ 识别（mobile_scanner）+ 历史；**视觉焕新**：顶部 **PageBanner 琥珀(3)**（顶部 Tab 用 `JianliSegmented` 生成/识别/历史，包进 Expanded，`expands:true` 雷区照旧）+ 历史页 `_QrHistoryTile`（琥珀图标盘 + 来源标识 + 时间格式化 `yyyy-MM-dd HH:mm`）+ StaggerList；**生成 Tab 四分组卡片（方案 A）**：① 类型 `SheetChoiceChip` wrap ② 内容 `_buildFields()` ③ 样式（前景/背景色板 swatch）④ 容错率 `JianliSegmented`（L·7% / M·15% / Q·25% / H·30%），`_styleSection` 已精简掉容错率；预览容器底色**跟随 `_bg`**（去白底 padding）+ 底部**三按钮一行** `Expanded×3` 等高 44（`TapScale` 包裹，lucide history/copy/share2，描边色随字色；存入历史·复制=渐变灰 `LinearGradient([card,muted])`+border，分享图片=主色渐变，全走主题色不硬编码）；**分享 PNG** 640×640 圆角 r24 卡片 + 48px 留白（`dart:ui` `PictureRecorder`/`Canvas`/`paintImage` 合成）；**识别去重** `onDetect` 用 `_lastScanned` 值去重 + `_scanSheetOpen` 弹窗占用锁，切回识别 Tab 清零 |
| sync | `/sync`（入口已收口到 `/backup`） | 扫描/手动 IP/推送/拉取，四种组合全通；**视觉焕新**：**PageBanner 青(5)**（发现设备/可同步表统计）+ 设备行图标青 `accent(5)` SquircleBox；**#昵称**：发现设备区上方「我的设备」卡片展示本机随机昵称（`localNickname`，同 file_transfer 行机制）。**2026-09-22 起并入「备份与恢复」页**：本页新增 `embedded` 模式（`SyncPage({embedded, bannerTitle, bannerSubtitle, bannerIcon})`），`embedded: true` 时只返回「收缩成不自滚的整块」正文（`shrinkWrap: true` + `NeverScrollableScrollPhysics`），由 `/backup` 的唯一滚动区承载；`embedded: false`（默认）行为与合并前逐字一致，`/sync` 仍可独立打开 |
| **backup** | `/backup` | **备份与恢复（2026-09-22 用户定案）= 原「数据同步」+「数据管理」合并**。顺序：数据同步段在前、数据管理段在后（中间 `SectionHeader('数据管理')` 分隔）。顶部横幅 = 同步段自带的 `PageBanner` 被改写（`bannerTitle: '备份与恢复'`、`bannerSubtitle: '局域网同步 · 数据库导入导出'`、`bannerIcon: FLucideIcons.databaseBackup`）；统计值「发现设备 / 可同步表」仍由同步段自身状态提供，故横幅**留在同步段内**、不搬进父页。**实现＝零业务搬迁**：`SyncPage` / `DataManagementPage` 各加 `embedded` 开关，父页 `lib/features/backup/backup_restore_page.dart` 只负责「单头部 + 单 `ListView`」骨架。**入口三处收口**：工具分组页一行「备份与恢复」（`databaseBackup`，青色 5）、首页快捷磁贴「备份 / 同步 · 恢复」、设置面板「数据与同步」分区单张 tile。旧路由 `/sync`、`/data-management` 保留（旧深链不失效）。⚠️ **本页是「多页连续排布」的首个先例**：父页持有唯一滚动区，子段用 `embedded` 返回不自滚整块（**禁止**在父级 `ListView` 里嵌套第二个可滚动 `ListView`）|
| **share_intake** | `/share-intake`（透明壳，不进分组页） | **P0-3 系统分享接收（2026-09-19）**：其他 App「分享到渐离」（ACTION_SEND text/plain）→ `receive_sharing_intent` 流 → `pendingShareProvider` 一次性意图 → 根组件 ref.listen push 透明壳 → md 档处理抽屉（预览 + 存为笔记/新建待办/用浏览器打开/复制）。取走即 `reset()`（不 reset 冷启动重复弹）；图片/文件分享 P1 再扩 mimeType。详见 changelog 2026-09-19「P0 系统级入口三件套」 |
| 系统入口三件套 | — | **P0 落地（2026-09-19）**：① 图标快捷方式 `quick_actions`（记笔记/扫码/开始专注/习惯打卡，`lib/app/shortcuts/app_shortcuts.dart::handleAppShortcut` 统一分发）；② 快捷磁贴「渐离打卡」= 原生 `JianliQuickTileService`（Android 14+ 用 PendingIntent 启动）→ MainActivity companion 暂存 extra → `SystemActionsChannel.takeQuickAction` 拉走（冷启动 postFrame + resumed 各轮询一次）；③ 桌面小组件「渐离·今日」`home_widget` + 原生 `TodayWidgetProvider`（快照键 `w_*` 双端契约，dashboard_statsProvider 变化即写，`lib/features/home/widget_snapshot.dart`）。 |
| **app_lock** | `/app-lock`（全屏拦截层，不进分组页） | **P1-1 应用锁（2026-09-19）**：生物识别解锁（`local_auth ^2.3.0`，**要求 MainActivity 为 FlutterFragmentActivity**）。状态机 `lib/app/security/app_lock.dart`（Notifier）：偏好存 basic_info（`app_lock_enabled` / `app_lock_grace`：'0' 立即（默认）/'60'/'300'）；冷启动 init 时 enabled→locked；`onPaused` 记时间戳、`onResumed` 按宽限期判锁。锁定转变沿由 app.dart `ref.listen` push `/app-lock`（NoTransitionPage，PopScope 禁返回）。设置入口：设置面板「应用锁」分区（开关 + 宽限期 JianliSegmented 立即/1分钟/5分钟）。 |
| **remote** | `/remote` | **P3-3 遥控 PC（2026-09-19）**：手机遥控电脑 = 翻页×2 / 黑屏(B 键) / **锁屏（长按 → `showSheetConfirm` 确认）** / 媒体×3 / 音量×3，共 10 命令 2 列网格。通信复用 47124 数据面 `/remote/*`（PC 端 `jianli-app/electron/main/module/remoteControl.ts`，**安全模型 = 4 位配对码**：手机请求配对 → PC `dialog.showMessageBox` 弹码（码不过网络）→ 手机提交码换 token（随机 32 hex，存 PC `basic_info(remote_pair_token)`、手机 `basic_info(remote_token_<ip>)`）→ 之后每条命令带 `x-remote-token` 头；403 = 失效自动清 token 引导重配；白名单命令在 PC 端硬编码）。服务层 `lib/features/remote/remote_service.dart`（HttpClient + 目标清单存 `remote_targets`），页面 `remote_page.dart`（扫描复用 `SyncDiscovery().scan()` 只列 electron 平台 + 手动 IP 兜底），入口在工具分组页。零新表。 |
| file_transfer | `/file-transfer` | 双端批量互传（PC⇆手机），与 sync 同协议同历史表 `file_transfer`（含 `error` 失败原因列）；接收目录 `Download/渐离App文件互传/`（Android 需存储权限「所有文件访问」，入页申请；未授权/创建失败回退沙盒 `Documents/渐离App文件互传/`，iOS 恒走沙盒回退）；sha256 双端校验；历史成功记录可「打开」（**底部弹层 `showFSheet`+`SheetSurface`**：打开所在文件夹 / 用应用打开，原生化 —— `MethodChannel('jianli/file_actions')`（`MainActivity.kt` 实现 `queryOpenableApps` 系统解析可打开应用+图标base64 / `openFolder` 打开所在文件夹 / `openWithApp` 用指定应用打开）+ 自持 `FileProvider` `${applicationId}.fileprovider`（`res/xml/file_paths.xml` 覆盖整外部存储）+ manifest `<queries>` 声明 `VIEW */*` 包可见性；Dart 封装 `services/file_actions.dart` 的 `FileActions`；**按钮文字仍是「打开」、图标 `folderOpen` 不变**）/「分享」(`share_plus`)；记录区只显示当次批次（`_batchTid` 过滤，其他历史暂不展示）；记录标题下方有存储位置提示行（授权→`Download/渐离App文件互传/`，未授权→提示点「分享」经其他应用保存，iOS→沙盒 Documents，`_receiveHint` 随入页授权流程刷新）；**增强（2026-09-06 二批）**：重名覆盖策略(rename/overwrite)、最近设备记忆(离线可见)、断点续传、会话加密(AES-256-CTR，默认关)、接收询问弹窗(关自动接收时，已改底部抽屉 `showFSheet`+`SheetSurface`+`GradientButton`「接收」主按钮，非破坏性按 UI 规范不用 `showFDialog`；`_AskReceiveSheet` 组件)、并发守卫(429)、后台保活(wakelock)、历史分页+自动清理；**视觉焕新**：**PageBanner 粉(4)**（收发统计）+ FDeterminateProgress 逐文件进度 + 设备扫描/手动 IP（模拟器 `10.0.2.2`）；**#昵称（随机设备名，2026-09-06 新增，三次细化）**：本机随机昵称便于识别收发双方。**基座格式** `【形容词1】【形容词2】的【名词】`（如「软萌俏皮的樱桃」）；词库三组（`lib/core/sync/device_nickname.dart` 的 `_nickAdj1/_nickAdj2/_nickNoun`，与 PC `transferModule.ts` 同源 `随即词库.md`）+ `ensureNickname()` 持久化（shared_preferences 键 `device_nickname`/`device_nickname_ts`，**首次生成后永久留存、不再轮换**（2026-09-08 改，原 3 天轮换已移除）；**迁移**：旧格式（带 `的渐离App`/`的App`/`的PC`/` (App)`/` (PC)` 任一后缀）一律立即重生为新基座格式）。`main()` 启动 `await ensureNickname()` 预载。**平台后缀规则**：本机展示（首页顶部「渐离」、同步页/文件互传页「我的设备」卡片）只用 `localNickname`（基座、**不带任何后缀**）；广播给对端的场景（`startServer`/`startResponder`/offer `me`/发送 `from` 应答，即 `sync_service`/`sync_discovery`/`transfer_server`/`transfer_client`）改取 `localBroadcastName` = `${localNickname}的App`，因此发现设备/历史设备列表里的对端名带 `的App` 后缀以便区分平台。|
| ferry | `/ferry` | **流光扫传**（原名「隔空互传」，2026-09-22 双端统一更名）：屏幕二维码 → 摄像头直传（QRFerry 本地 WebView，≤10MB；与 PC 同机制共享资源）。⚠️ **代码代号 `ferry` / 路由 `/ferry` / 端点 `/ferry-save` / 第三方静态站 `qyferry`(QRFerry) / 落盘目录 `Download·Documents/渐离App隔空互传` 全部刻意保留旧名**（改目录会让老用户已收件「消失」），双端注释里已写明缘由。工具页条目名「流光扫传」、副标题「屏幕二维码 → 摄像头直传」 |
| **note_slip** | `/slip` | **P1-6 小纸条（2026-09-19，双端同名【小纸条】）**：PC⇄手机 文字/链接速传（单条 ≤8000 字），大文件仍走文件互传。协议**复用 47124 数据面**（`registerRouteHandler`）新增两端点 `GET /slip/ping` + `POST /slip/push`（body `{id,from:{name,platform},kind,content,ts}`，`id` 即幂等键，重复推送只更新不重复弹通知），发现复用 47123（platform：`win32-electron` ⇄ `android`/`ios`）。新表 **`note_slip`**（TEXT 主键 `key`，`direction in|out` / `kind text|url` / `title` / `content` / `peer_name` / `peer_ip` / `read` / `created_at`），**schemaVersion 5→6**，**不入同步白名单**（收发记录是本机行为，同步会把两端记录互相灌入对方收件箱）。代码 `lib/features/note_slip/`：`models/note_slip.dart` + `repositories/note_slip_repository.dart` + `services/note_slip_server.dart`（接收）+ `services/note_slip_client.dart`（发送）+ `providers/note_slip_providers.dart` + `note_slip_bootstrap.dart`（**接收常驻开关**）+ `note_slip_intake.dart`（通知点击落点）+ `components/note_slip_page.dart` / `note_slip_detail_sheet.dart`。⚠️ **三条铁律**：① **接收必须起服务**——`startServer + startResponder` 幂等调用，此前只有打开同步/互传页才起，现在由 `note_slip_bootstrap.dart` 按开关在**冷启动**（`app.dart` initState）或**仅开页面时**拉起；② **`registerRouteHandler` 必须幂等注册**（加守卫布尔），否则一次请求被处理两遍、弹两条通知；③ 新通知渠道 **`slip`**（High + `NotificationPrivacy.Public`，**渠道重要性创建后锁定**，键名不可再改），点击经全局 sink 跳 `/slip?key=` 打开详情抽屉（复制 / 浏览器打开 / 存笔记 / 建待办，落点复用 share_intake）。入口：工具分组页 `hub_pages.dart`。**内容模式（2026-09-21）**：底部发送条单行输入写不下长文本 → **maximize2 按钮 / 长按输入框** 弹 lg 抽屉 `_ComposeSheet`（无边框沉浸多行正文对齐新增笔记页方案 B 正文区，底部取消/发送；controller 自持、打开不 autofocus）；发送直接走 `_sendText` 共用核心、成功清底部草稿，取消把编辑结果回填输入框（X/遮罩不动草稿）。 |
| **browser** | `/browser` | **移动端专有**（PC 无浏览器模块）：完整 Via 风浏览器 = 极简搜索首页（新标签页 = **图标标题 + 搜索栏 + 2×4 固定标签**，用户定案对应 Chrome/Edge 新标签页）＋**单 WebView 多标签**（切标签 = 换地址 `loadUrl`，空白标签 `about:blank` + 首页覆盖层）＋地址栏两态（常态域名 / 输入态淡紫底 r22 + 取消）＋底部工具栏（返回/前进/**首页**（原「刷新」位，已定案交换）/标签/菜单）。引擎 `flutter_inappwebview ^6.1.5`（原为 `flutter_epub_viewer` 传递依赖，已提为直接依赖、零新增下载；与隔空互传的 `webview_flutter` 两套共存）。**4 张专有表**（`browser_tabs` / `browser_pinned` / `browser_bookmarks` / `browser_history`，schemaVersion **4**，**不入同步白名单**）；设置存 `basic_info`（`browser_*` 前缀）。**Phase 1 + Phase 2 均已落地（2026-09-19）**：Phase 2 = 书签页（文件夹树+长按） / 历史页（按天分组+长按） / 完整设置页（7 分组） / 固定标签页管理 / **三级广告拦截**（内置静态种子集 + 可订阅 + 自定义，`services/adblock_*.dart`） / 清除浏览数据（时间范围+勾选项）；**8 条顶层子路由**。Phase 3 待做 = 真 favicon / 会话恢复打磨 / 下载管理 / 完整 EasyList 打包。详见 **`modules/browser.md`** + `references/browser-plan.md`（设计稿 <https://ardot.tencent.com/file/727382358962697> 7 屏）|
| screenshots | — | 未开工；移动端无法系统级监听截图，重设计为相册导入/分享收纳 |
| 主题 | — | **9 套主题样式**（渐离紫 `zi` / 远峰蓝 `blue` / 森野绿 `green` / 落日橙 `orange` / 樱粉 `pink` + 正色四套（2026-09-10 取消莫兰迪灰调，改 bright 正色贴合名字）：丹枫红 `red` `0xFFE5484D` / 秋香黄 `yellow` `0xFFF4B400` / 远山青 `cyan` `0xFF0EA5A4` / 霜月白 `white` `0xFF9DB4CC`（月光银蓝，纯白无法做按钮底色），`AppTheme.styles`）+ 三态模式（跟随系统/浅色/深色）+ **阅览模式**（普通/大号正文字体，卡片 tag 切换），均 SharedPreferences 持久化；切换入口在首页与三个分组页右上角的**设置按钮 → 左侧设置面板**。桌面 25 套 token 映射 P2 |

## 新增功能域落地清单
1. 建 `lib/features/<module>/`（models / repositories / providers / components 按需原子拆分，带中文注释）。
2. 需要数据表：按「drift 三大铁律」加表定义并注册，跑 build_runner，老用户写 onUpgrade 迁移。
3. 页面 UI 按「UI 体系（forui）」章节的骨架与组件对照表编写；在 `lib/app/router/app_router.dart` 加路由，并在 `lib/features/hubs/hub_pages.dart` 对应分组页加入口（对应桌面端「侧边栏菜单 + 可见开关」的移动端做法）。
4. 涉及到点提醒：在 `core/notifications/notification_service.dart` 的 `NotificationChannels` 注册渠道（`habit`/`todo`/`pomodoro`/`countdown`/`alarm`），`reminders` → 通知计划翻译（`ReminderRepository.scheduleNotification` 按 `mode`+`delivery` 路由：`scheduleCalendar`/`scheduleInterval`/`scheduleOnce`；`alarm` 走 preciseAlarm+fullScreenIntent+alarm 渠道；`notification` 走普通渠道+allowWhileIdle）。⚠️ 闹钟的 preciseAlarm 需 Android 12+ `SCHEDULE_EXACT_ALARM`（已在 AndroidManifest 声明；运行时可用性用 `NotificationService.exactAlarmAllowed` 检查、`requestExactAlarmPermission()` 引导去系统设置，Android 13+ 通知权限还需 `POST_NOTIFICATIONS`——也已在 manifest 声明）；iOS 无等价 AlarmManager，闹钟退化为高优通知。改动 `reminders` 表（如加 `delivery` 列）须 `schemaVersion+1` + `onUpgrade` `addColumn`，并 `dart run build_runner build -d` 重新生成 `app_database.g.dart`。⚠️ **到点通知的兜底（2026-09-13 重定，拆两个入口）**：`lib/app/alarm_bootstrap.dart` —— `bootstrapAlarms(db)`（**只在冷启动首帧**跑：申请通知权限 + `healAlarmSchedules` + `KeepAliveGuard.ensureRunning`）与 `healAlarmSchedules(db)`（**只做自愈**：提醒 `rescheduleAll` + 倒计时 `sweepExpired`，**不申请权限**，可反复调用）；`app.dart` 的 `didChangeAppLifecycleState` 在 `resumed` 分支按 **5 分钟节流**调 `healAlarmSchedules` → ROM 清掉原生计划后「随手打开一次 App 就自动恢复」。新增功能域如需后台自愈，**把逻辑塞进 `healAlarmSchedules`**，别再各挂一份、也别挂进回前台路径去申请权限。**番茄钟已于 2026-09-13 改为页面内计时，无启动步骤**。⚠️ **提醒可靠性横切（用户报「切/关 App 后系统提醒失效」）**：四项系统开关统一由 `NotificationService.checkCapabilities()` 判定（通知 / 精确闹钟 / 忽略电池优化 / 全屏通知）+ `_ReminderGuardSheet` 一键修复；原生能力（忽略电池优化查询、全屏通知跳转、前台服务保活）走自建通道 `jianli/system_actions`（`android/.../SystemActionsChannel.kt` + `ReminderKeepAliveService.kt`，Dart 封装 `lib/core/android/system_actions.dart`），manifest 补 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` / `FOREGROUND_SERVICE` / `FOREGROUND_SERVICE_SPECIAL_USE`。完整规格与坑见 **SKILL.md 红线 #16**。⚠️ **awesome_notifications 0.12.1 API 三坑（2026-09-09 实测踩过）**：① `actionButtons` 是 `createNotification` 的参数，**不是** `NotificationContent` 的（写进 content 直接「No named parameter」）；② `setListeners` 的回调参数名是 **`onActionReceivedMethod`**（没有 `onActionReceived`），且**必须传静态/顶层函数引用**、绝不能传闭包——插件内部用 `PluginUtilities.getCallbackHandle` 取句柄，闭包取不到会静默不触发回调；③ `ReceivedAction.id` 类型是 `int?`，参与算术前要 `?? 0`。⚠️ `ReminderItem` 新增字段须同步改 **4 处**：构造器 `required this.x` / `fromRow` / `copyWith`（参数 **+** body）/ `ReminderRepository._toCompanion`，漏任何一处都编译不过（2026-09-09 加 `month`/`dayOfMonth` 时踩过；注意 `month`/`dayOfMonth` 是表已有列，故无需再跑 build_runner）。⚠️ 反之，**给 `reminders` 表新加列（如 `delivery`）必须 `dart run build_runner build -d`** —— 没重生成的 g.dart 会同时炸三处：`addColumn` 报「`Column<String>` 不能赋给 `GeneratedColumn`」（因为列 getter 回落成 Table 类的声明）、`row.xxx` 报 getter 未定义、`RemindersCompanion` 报「No named parameter」。这三处报错**不是代码写错，是 g.dart 过期**，先跑 build_runner 再排查。
5. 需要双端同步：默认 TEXT 主键（INTEGER 主键表参考「主题对话」的 pk 按表适配先例），同时加移动端 `kSyncableTables` 与桌面端 `syncModule.ts` 白名单（PC UI `useSync.ts` 的 `SYNC_TABLES` 别漏）。
6. `flutter analyze` + `flutter test` 过基线，更新本 SKILL.md 的功能域清单。

## 使用方式
1. 接到任务先判断属于「数据 / 加密 / 同步 / UI / 构建」哪一类，读对应章节。
2. 涉及桌面端契约（表结构、加密信封、同步对端协议）时，读桌面技能 `C:\cod\jianli\jianli-app\.workbuddy\skills\jianli-app\` 下对应文档（`references/flutter-port.md`、`references/modules/sync.md`、`references/modules/file-transfer.md` 等）。
3. 新增能力优先复用既有模式：Riverpod provider 拆分、幂等 upsert、NotificationChannels、sync 白名单，不要另起炉灶。
4. 所有文档用中文；发现与代码不符，直接更新对应文档，保持 skill 与代码同步。

